name: test
channels:
 - conda-forge
 - defaults
dependencies:
   # Base depends
 - python
 - pip

   # Package dependencies
 - numpy

   # Testing
 - pytest
 - pytest-cov
 - codecov

   # Pip-only installs
 - pip:
   - qiskit
   - sympy
   - matplotlib
   - ipython
   - pylatexenc
   - networkx